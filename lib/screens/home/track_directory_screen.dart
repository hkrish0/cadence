import 'package:flutter/material.dart';

class TrackDirectoryScreen extends StatelessWidget {
  const TrackDirectoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Track Directory — TODO')),
    );
  }
}
